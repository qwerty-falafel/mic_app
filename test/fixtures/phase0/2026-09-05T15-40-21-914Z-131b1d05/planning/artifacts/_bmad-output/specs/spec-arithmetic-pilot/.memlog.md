---
topic: Dependency-free JavaScript ES module arithmetic.js with add/subtract/multiply/divide for finite numbers, node:test tests
updated: 2026-09-05T16:54
---

- (direction) Headless run, express mode (headless default); slug arithmetic-pilot supplied by caller; planning only, no implementation
- (capability) CAP-1: module exports add(a,b), subtract(a,b), multiply(a,b) returning the arithmetic result for finite number inputs; success: node:test assertions on known operands pass
- (capability) CAP-2: module exports divide(a,b) returning the quotient for finite numbers and throwing RangeError when the divisor is zero; success: node:test asserts a known quotient and that divide(x,0) throws RangeError
- (constraint) Dependency-free: no runtime dependencies of any kind
- (constraint) Form: a single JavaScript ES module file named arithmetic.js (not CommonJS, no build step implied)
- (constraint) Domain: inputs are finite numbers; divide reports a zero divisor by throwing RangeError (a specific error type, not a generic one)
- (constraint) Tests use node:test (the Node built-in test runner)
- (direction) Non-goals from input: no CLI, no UI, no persistence, no arbitrary precision
- (assumption) Non-finite inputs (NaN, +/-Infinity) are outside the stated finite-number domain; behavior is unspecified and untested
- (question) Where do arithmetic.js and its test file live in the project (repo root, src/, or elsewhere)?
- (question) Should the operations throw on non-number inputs, or is that behavior unspecified?
- (decision) Success signal: the node:test suite for arithmetic.js passes, covering all four exports including RangeError on divide(x, 0)
- (decision) No companions needed: no diagrams, catalogs, or reference material beyond kernel-shape lines; stack facts stay in Constraints
